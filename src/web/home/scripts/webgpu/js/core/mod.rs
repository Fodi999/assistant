// ── core/ — WebGPU runtime: init, state, buffers, pipeline, render loop ──────

pub mod init;
pub mod state;
pub mod buffers;
pub mod pipeline;
pub mod render_loop;
pub mod perf_hud;
pub mod geometry_bridge;
