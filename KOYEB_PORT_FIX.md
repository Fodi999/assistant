# 🔧 Koyeb Port Configuration Fix

## Проблема
Сервер запускается успешно и слушает на порту **8000**, но Koyeb возвращает 404 ошибку, потому что настроен на порт **8080**.

## Решение через Koyeb UI

### Шаг 1: Откройте Koyeb Dashboard
1. Зайдите на https://app.koyeb.com
2. Откройте ваш сервис `restaurant-backend`

### Шаг 2: Обновите порт в настройках
1. Перейдите в **Settings** → **Ports**
2. Измените Public Port с `8080` на `8000`
3. Или в секции **Exposed Ports** убедитесь, что указан порт `8000`

### Шаг 3: Проверьте переменные окружения (опционально)
1. Перейдите в **Settings** → **Environment**
2. Найдите переменную `PORT`
3. Если она установлена как `8080`, измените на `8000`
4. Если переменной нет - оставьте как есть (будет использоваться из Dockerfile)

### Шаг 4: Сохраните и дождитесь перезапуска
1. Нажмите **Save** или **Update Service**
2. Дождитесь перезапуска сервиса (1-2 минуты)
3. Проверьте логи, что сервер запустился на `0.0.0.0:8000`

## Проверка после исправления

```bash
# Проверка health endpoint
curl https://restaurant-backend-dmitrijfomin-fodi999.koyeb.app/health

# Тест админского логина
curl -X POST "https://restaurant-backend-dmitrijfomin-fodi999.koyeb.app/api/admin/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@fodi.app","password":"Admin123!"}'
```

Должен вернуться JSON с токеном, а не HTML страница 404.

## Текущее состояние

- ✅ Dockerfile настроен правильно (EXPOSE 8000)
- ✅ koyeb.yaml обновлен (порт 8000)
- ✅ Сервер запускается успешно на порту 8000
- ❌ Koyeb роутинг настроен на порт 8080 (требует ручного исправления в UI)

## Альтернативное решение

Если UI недоступен, можно изменить порт в коде:
1. Обновить все конфигурации на порт 8080
2. Пересобрать и передеплоить

Но это неправильный подход, так как 8000 - стандартный порт для этого приложения.
