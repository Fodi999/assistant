# 📋 КРАТКАЯ ШПАРГАЛКА: User Search + Inventory

**Одна страница для быстрого старта** 📄  
**На русском** 🇷🇺  
**Copy-Paste ready** ✅

---

## 🎯 Суть

**ЦЕЛЬ**: Дать пользователю возможность:
1. 🔍 Поискать товар в каталоге (молоко, говядина, и т.д.)
2. ➕ Добавить найденный товар на свой склад
3. 📦 Просмотреть свой склад с датами истечения
4. 🗑️ Удалить товар со склада

**ЯЗЫК**: Автоматически из профиля пользователя (БД) → Результаты на русском/английском/польском/украинском

**ДАТА ИСТЕЧЕНИЯ**: Автоматически = дата поступления + срок годности из каталога

---

## 📁 Файлы для Создания (6 штук)

### 1. Типы
**Файл**: `src/types/catalog.ts`  
**Размер**: ~50 строк  
**Источник**: USER_CATALOG_SEARCH_CODE.md → "Types"  
**Копировать**: ВСЕ, с interface Ingredient до interface AddToInventoryInput

### 2-3. Hooks (два файла)
**Файл 1**: `src/hooks/useCatalogSearch.ts`  
**Размер**: ~50 строк  
**Источник**: USER_CATALOG_SEARCH_CODE.md → "Hook 1"

**Файл 2**: `src/hooks/useInventory.ts`  
**Размер**: ~100 строк  
**Источник**: USER_CATALOG_SEARCH_CODE.md → "Hook 2"

### 4. Компонент
**Файл**: `src/components/UserCatalogSearch.tsx`  
**Размер**: ~350 строк  
**Источник**: USER_CATALOG_SEARCH_CODE.md → "Компонент 1"  
**Копировать**: ВСЕ, от 'use client' до последней строки

### 5-6. Страницы (две страницы)
**Файл 1**: `src/app/catalog/page.tsx`  
**Размер**: ~40 строк  
**Источник**: USER_CATALOG_SEARCH_CODE.md → "Страница 1"

**Файл 2**: `src/app/inventory/page.tsx`  
**Размер**: ~150 строк  
**Источник**: USER_CATALOG_SEARCH_CODE.md → "Страница 2"

---

## ⚡ Быстрый Старт (30 мин)

```bash
# 1. Создать файлы (copy-paste из USER_CATALOG_SEARCH_CODE.md)
#    Можно создавать через IDE, не нужно ничего писать!

# 2. Установить зависимость (1 строка)
npm install lodash @types/lodash

# 3. Запустить (два терминала)
# Terminal 1:
cargo run --release

# Terminal 2:
npm run dev

# 4. Тестировать
# Браузер: http://localhost:3000/catalog
# Поиск: "молоко" ← должно найти "Пастеризованное молоко" на русском ✅
```

---

## 🔌 API Endpoints (3 штуки)

```bash
# 1. ПОИСК
GET /api/catalog/ingredients?q=молоко
# → returns: ingredients array с name на русском

# 2. ДОБАВЛЕНИЕ НА СКЛАД
POST /api/inventory/products
{
  "catalog_ingredient_id": "uuid",
  "quantity": 5.0,
  "price_per_unit_cents": 10000,
  "received_at": "2026-02-15T10:30:00Z"
}
# → returns: 201 CREATED с expires_at (автовычислено!)

# 3. ПРОСМОТР СКЛАДА
GET /api/inventory/products
# → returns: array товаров со статусом истечения (Fresh, Expired, и т.д.)
```

**Ключевое**: Язык автоматический! Не нужно передавать параметр `?lang=ru`  
**Ключевое**: Дата истечения вычисляется автоматически! Не нужно вводить пользователю!

---

## 🎨 UI Поток

```
/catalog (поиск)
    ↓
[поиск: "молоко"] → Результаты: "Пастеризованное молоко"
    ↓
[Нажал "➕ Добавить"]
    ↓
[Модаль с формой]
    ├─ Кол-во: ___
    ├─ Цена: ___
    ├─ Дата: ___
    └─ [✅ Добавить]
    ↓
/inventory (мой склад)
    ↓
[Товар в списке со статусом "Fresh"]
```

---

## 🧪 Проверка (4 теста)

```
✅ Тест 1: Поиск на русском
   http://localhost:3000/catalog
   Введи: "молоко"
   Ожидаемо: Результаты на русском языке

✅ Тест 2: Добавление
   Нажми "➕ Добавить"
   Заполни форму (5, 10000, сегодня)
   Нажми "✅ Добавить"
   Ожидаемо: Toast "✅ Добавлено!"

✅ Тест 3: Просмотр
   Перейди на http://localhost:3000/inventory
   Ожидаемо: Товар там со статусом "Fresh"

✅ Тест 4: Дата вычисляется
   В /inventory проверь: дата = сегодня + 5 дней
   (потому что default_shelf_life_days молока = 5)
```

**Если все 4 зелёные** → Production Ready! 🚀

---

## 📊 Что происходит внутри

```
Frontend → GET /api/catalog/ingredients?q=молоко
Backend:
  1. Получить user.language из токена (например, 'ru')
  2. Поискать в БД WHERE language='ru'
  3. Вернуть результаты на русском
Frontend ← [{"name": "Пастеризованное молоко", ...}]

───────────────────────

Frontend → POST /api/inventory/products
          {catalog_ingredient_id, quantity, price, received_at}
Backend:
  1. Валидировать (quantity > 0, price >= 0)
  2. Загрузить default_shelf_life_days из каталога (= 5)
  3. Вычислить: expires_at = received_at + 5 дней
  4. Сохранить в БД
Frontend ← {id, ingredient_name, expires_at: "2026-02-20", ...}

───────────────────────

Frontend → GET /api/inventory/products
Backend:
  1. Загрузить все товары пользователя
  2. JOINить с переводами (на его языке)
  3. Вычислить expiration_status (Fresh/Expired/и т.д.)
Frontend ← [{name: "Пастеризованное молоко", status: "Fresh", ...}]
```

---

## 🔑 Ключевые Особенности

| Что | Кто решает | Где |
|-----|-----------|-----|
| **Язык** | Backend (из БД) | user.language |
| **Дата истечения** | Backend (автоматически) | received_at + default_shelf_life_days |
| **Цена** | Frontend (пользователь) | Modal form |
| **Количество** | Frontend (пользователь) | Modal form |
| **Изображение** | Backend (из каталога) | catalog_ingredients.image_url |
| **Статус истечения** | Backend (вычисляется) | expired, fresh, expiring_soon |

---

## 🚀 Деплой на Koyeb

```bash
git add .
git commit -m "Add user catalog search and inventory"
git push origin main

# Koyeb автоматически перестроит (2-3 мин)
# Затем тестируй на: https://ваш-фронтенд-koyeb.url/catalog
```

---

## 🛠️ Если Что-то Не Работает

| Проблема | Решение |
|----------|---------|
| "Cannot find module 'lodash'" | `npm install lodash @types/lodash` |
| Поиск = 0 результатов | Проверь: `SELECT language FROM users` (должен быть 'ru') |
| Дата не вычисляется | Проверь: `default_shelf_life_days` в БД не NULL |
| API возвращает 401 | Проверь токен в localStorage |
| TypeScript ошибки | Проверь что types/catalog.ts создан правильно |

---

## 📚 Откуда Копировать Код

```
USER_CATALOG_SEARCH_CODE.md
├── 📦 Types: types/catalog.ts
├── 🪝 Hook 1: useCatalogSearch.ts
├── 🪝 Hook 2: useInventory.ts
├── 🎨 Компонент 1: UserCatalogSearch.tsx
├── 🖼️ Страница 1: /app/catalog/page.tsx
└── 📦 Страница 2: /app/inventory/page.tsx

ВСЕ КОД ГОТОВ К COPY-PASTE! ✅
```

---

## ✨ Результат

После 30 минут работы:

✅ Пользователь может поискать "молоко"  
✅ Получит результаты на русском языке  
✅ Нажать "Добавить"  
✅ Заполнить форму  
✅ Добавится на склад автоматически  
✅ Дата истечения вычислится автоматически  
✅ Можно просмотреть свой склад  
✅ Можно удалить товар  

**100% Production Ready!** 🎉

---

## 📞 Остались Вопросы?

- **Детальное объяснение**: USER_CATALOG_SEARCH_INVENTORY_COMPLETE.md
- **Визуальные диаграммы**: USER_SEARCH_QUICK_VISUAL_GUIDE.md
- **Полный чек-лист**: USER_SEARCH_IMPLEMENTATION_CHECKLIST.md
- **Готовый код**: USER_CATALOG_SEARCH_CODE.md
- **Быстрый старт**: USER_SEARCH_30MIN_QUICKSTART.md

---

**Дата**: 15 февраля 2026  
**Время реализации**: 30 минут  
**Сложность**: Очень легко (Copy-Paste)  
**Результат**: Production-ready система 🚀

*Enjoy your new feature!* 🎊
