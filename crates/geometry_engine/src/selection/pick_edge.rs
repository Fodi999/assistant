//! Pick the nearest topology edge under a ray.
#![allow(dead_code, unused_variables, unused_imports)]
use crate::{brep::BrepModel, math::Ray, topology::EdgeId};

pub fn pick(_model: &BrepModel, _ray: &Ray) -> Option<EdgeId> { todo!() }

