//! Pick the nearest topology vertex under a ray.
#![allow(dead_code, unused_variables, unused_imports)]
use crate::{brep::BrepModel, math::Ray, topology::VertexId};

pub fn pick(_model: &BrepModel, _ray: &Ray) -> Option<VertexId> { todo!() }

