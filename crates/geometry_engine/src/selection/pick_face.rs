//! Pick the topology face under a ray.
#![allow(dead_code, unused_variables, unused_imports)]
use crate::{brep::BrepModel, math::Ray, topology::FaceId};

pub fn pick(_model: &BrepModel, _ray: &Ray) -> Option<FaceId> { todo!() }

