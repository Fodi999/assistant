//! Offset a B-Rep face perpendicular to its surface.
#![allow(dead_code, unused_variables, unused_imports)]
use crate::{brep::BrepModel, math::Real, topology::FaceId};

pub fn run(_model: &mut BrepModel, _face: FaceId, _distance: Real) { todo!() }

