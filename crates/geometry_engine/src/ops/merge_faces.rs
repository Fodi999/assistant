//! Merge two coplanar faces sharing an edge.
#![allow(dead_code, unused_variables, unused_imports)]
use crate::{brep::BrepModel, topology::FaceId};

pub fn run(_model: &mut BrepModel, _a: FaceId, _b: FaceId) { todo!() }

