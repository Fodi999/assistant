//! Extrude an existing planar face of a B-Rep body.
#![allow(dead_code, unused_variables, unused_imports)]
use crate::brep::BrepModel;
use crate::topology::FaceId;
use crate::math::Real;

pub fn run(_model: &mut BrepModel, _face: FaceId, _distance: Real) { todo!() }

