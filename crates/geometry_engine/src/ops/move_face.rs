//! Move a face along a vector (parametric edit).
#![allow(dead_code, unused_variables, unused_imports)]
use crate::{brep::BrepModel, math::Vec3, topology::FaceId};

pub fn run(_model: &mut BrepModel, _face: FaceId, _v: Vec3) { todo!() }

