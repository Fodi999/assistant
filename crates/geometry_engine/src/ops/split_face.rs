//! Split a face by an imprint curve.
#![allow(dead_code, unused_variables, unused_imports)]
use crate::{brep::BrepModel, topology::FaceId};

pub fn run(_model: &mut BrepModel, _face: FaceId) { todo!() }

